name: coral

x-common-env: &common-env
  SUPABASE_URL: http://host.docker.internal:54321
  SUPABASE_ANON_KEY: sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH
  SUPABASE_TABLE: player_progress
  DEFAULT_PLAYER_ID: local-player

x-common-host: &common-host
  extra_hosts:
    - "host.docker.internal:host-gateway"

services:
  web:
    build:
      context: .
      dockerfile: docker/web.Dockerfile
    working_dir: /workspace/web
    command: sh -c "yarn install && yarn dev"
    ports:
      - "3000:3000"
    environment:
      <<: *common-env
    volumes:
      - ./:/workspace
      - web_node_modules:/workspace/web/node_modules
    <<: *common-host

  bridge:
    build:
      context: .
      dockerfile: docker/bridge.Dockerfile
    working_dir: /workspace
    command: sh -c "node bridge/server.js"
    ports:
      - "8787:8787"
    environment:
      <<: *common-env
      BRIDGE_PORT: 8787
    volumes:
      - ./:/workspace
      - root_node_modules:/workspace/node_modules
    <<: *common-host

  native-metro:
    build:
      context: .
      dockerfile: docker/native.Dockerfile
    working_dir: /workspace
    command: sh -c "yarn install && yarn start"
    ports:
      - "8081:8081"
    environment:
      <<: *common-env
    volumes:
      - ./:/workspace
      - root_node_modules:/workspace/node_modules
    <<: *common-host

  electron:
    profiles: ["desktop"]
    build:
      context: .
      dockerfile: docker/electron.Dockerfile
    working_dir: /workspace
    command: sh -c "yarn --prefix electron install && yarn --prefix electron dev"
    environment:
      <<: *common-env
      WEB_URL: http://web:3000
      DISPLAY: ${DISPLAY:-}
    volumes:
      - ./:/workspace
      - electron_node_modules:/workspace/electron/node_modules
      - /tmp/.X11-unix:/tmp/.X11-unix
    depends_on:
      - web

  sprites:
    profiles: ["tools"]
    build:
      context: .
      dockerfile: docker/sprites.Dockerfile
    working_dir: /workspace
    command: sh -c "pip install --no-cache-dir -r tools/sprites/requirements.txt && python tools/sprites/generate_sprites.py --out-dir tools/sprites/out"
    volumes:
      - ./:/workspace

  godot-test:
    profiles: ["test"]
    platform: linux/amd64
    build:
      context: .
      dockerfile: docker/godot-test.Dockerfile
    working_dir: /workspace
    command: ["--script", "res://tests/e2e_runner.gd"]
    environment:
      <<: *common-env
    volumes:
      - ./:/workspace

  test-suite:
    profiles: ["test"]
    platform: linux/amd64
    build:
      context: .
      dockerfile: docker/tests.Dockerfile
    working_dir: /workspace
    command: ["./scripts/test-all.sh"]
    environment:
      <<: *common-env
    volumes:
      - ./:/workspace
      - root_node_modules:/workspace/node_modules
    <<: *common-host

volumes:
  web_node_modules:
  root_node_modules:
  electron_node_modules:
